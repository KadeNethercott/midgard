'use strict';
/*
describe('service', function() {
  beforeEach(module('harmonia.services'));

  describe('version', function() {
    it('should return current version', inject(function(version) {
      expect(version).toEqual('0.1');
    }));
  });

  describe('Plugin', function() {
    var $httpBackend;

    beforeEach(inject(function(_$httpBackend_) {
      $httpBackend = _$httpBackend_;
    }));

    it('should return a resource', inject(function(Plugin) {
      var body = [{name: "one"}, {name: "two"}];
      $httpBackend.expectGET('/midgard/admin/pluginList')
        .respond(body);
      var result = Plugin.query();
      $httpBackend.flush();
      expect(result.count).toEqual(body.count);
    }));
  });
});
*/